import React from 'react';
import { Form } from 'react-bootstrap';

const ViewByAuthor = () => {
  return (
    <Form.Select className="mb-3">
      <option>Автору</option>
    </Form.Select>
  );
};

export default ViewByAuthor;
